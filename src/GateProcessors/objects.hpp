#pragma once

#include <vector>

/* component from 0 to 6
 * */
struct gates {

};

struct CustomComponent {

};

/*
 * int is a type of the component where
 * it contain the  object types
 * */
struct ComponentGroup{
  std::vector<int> process_group;


};



